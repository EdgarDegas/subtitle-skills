# Source-only Skill: run main.py directly; no build backend or installed package.
[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py39"
line-length = 110
